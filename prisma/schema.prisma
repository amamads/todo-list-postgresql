datasource db {
  provider = "postgresql"
}
generator client {
  provider = "prisma-client-js"
  output = "./generated"
}

model Todo {
  id     Int     @id @default(autoincrement())
  isDone Boolean
  text   String
}
